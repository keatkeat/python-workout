import random

the_number = random.randint(10,30)

def guessing_game():
	# Choose random integer between 0 and 100 (inclusive)
	# Ask user to guess what number has been chosen
	guess = input('Enter your guess: ')
	# for guess in guesses:
		# guess is too high... try again
		# guess is too low... try again
		# guess is just right... exit
